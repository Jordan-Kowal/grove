# Used by `task release:local` to produce a signed + notarized DMG.
# Copy this file to `.env` (gitignored) and adjust if needed.
# Same values are hardcoded in .github/workflows/build-and-release.yml for CI.

SIGN_IDENTITY="Developer ID Application: Jordan Kowal (DZZL8SY62B)"
NOTARY_PROFILE="grove-notary"
